{
  "host": "${process.env.SERVER_HOST || 'localhost'}",
  "port": "${process.env.SERVER_PORT || 1337}",
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
